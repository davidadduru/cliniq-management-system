<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clinic Management System</title>
    <style>
        body {
            background: linear-gradient(to bottom, #e0f7fa, #80deea); /* Lighter gradient background */
            color: #333;
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            position: relative;
        }

        .container {
            text-align: center;
            background: rgba(255, 255, 255, 0.8); /* White with transparency */
            padding: 40px 50px;
            border-radius: 15px;
            box-shadow: 0px 6px 25px rgba(0, 0, 0, 0.1); /* Softer shadow */
            max-width: 400px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: center;
            backdrop-filter: blur(10px); /* Less blur for a more subtle effect */
        }

        .logo img {
            width: 120px;
            margin-bottom: 20px;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 15px;
            color: #001f3f;
            text-shadow: 0px 3px 5px rgba(0, 0, 0, 0.1);
        }

        p {
            font-size: 1.1rem;
            margin-bottom: 20px;
            color: #333;
            line-height: 1.5;
        }

        .buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .buttons a {
            text-decoration: none;
            color: #fff;
            background: #00acc1; /* Soft blue */
            padding: 12px 25px;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: bold;
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .buttons a:hover {
            background: #00838f; /* Darker blue */
            transform: translateY(-3px);
            box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.2);
        }

        footer {
            position: absolute;
            bottom: 10px;
            text-align: center;
            font-size: 0.85rem;
            width: 100%;
            color: #666;
        }

        footer span {
            color: #00acc1;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <img src="/assets/images/cliniq-removebg-preview.png" alt="Clinic Logo">
        </div>
        <h1>Welcome to ClinIQ</h1>
        <p>Streamline your clinic management with ease and efficiency!</p>
        <div class="buttons">
            <a href="/login">Log in</a>
            <a href="/register">Register</a>
        </div>
    </div>
    <footer>
        <p>© 2024 <span>AUF CCS</span> | Clinic Management System</p>
    </footer>
</body>
</html>
